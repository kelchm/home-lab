---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana-mcp
spec:
  # Provides the PrometheusRule CRD. Grafana remains this MCP server's only
  # runtime application dependency.
  dependsOn:
    - name: prometheus-operator-crds
      namespace: observability
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: grafana-mcp
      namespace: ai
  interval: 1h
  path: ./kubernetes/apps/ai/grafana-mcp/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: ai
  wait: false
