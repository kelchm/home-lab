---
# Default-deny shape: only MCP gateways in the ai namespace reach the
# pcbparts-mcp HTTP transport. Public-only HTTP(S) egress and DNS are supplied
# by the namespace-level ai-public-web-egress policy.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: pcbparts-mcp
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: pcbparts-mcp
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: ai
            app.kubernetes.io/name: metamcp
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
