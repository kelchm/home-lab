---
apiVersion: v1
kind: Namespace
metadata:
  name: longhorn-system
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
  labels:
    # Relax enforcement only; retain Talos' restricted warn/audit defaults.
    pod-security.kubernetes.io/enforce: privileged
