---
# Routes longhorn.home.kelch.io to the longhorn-frontend Service via the
# admin gateway. BasicAuth is enforced by attaching the longhorn-ui-auth
# Middleware as a Gateway API ExtensionRef filter (Traefik resolves
# group=traefik.io/kind=Middleware natively).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-ui
spec:
  hostnames: ["longhorn.home.kelch.io"]
  parentRefs:
    - name: gateway-admin
      namespace: network
      sectionName: https
  rules:
    - filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: longhorn-ui-auth
      backendRefs:
        - name: longhorn-frontend
          port: 80
