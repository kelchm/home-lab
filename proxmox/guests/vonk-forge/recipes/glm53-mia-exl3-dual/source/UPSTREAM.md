# Source provenance

This adapter derives from [CarstVaartjes/vonk-forge-recipes at 5bbb0be4e604768499ccbcf82bbea181575c31d6](https://github.com/CarstVaartjes/vonk-forge-recipes/tree/5bbb0be4e604768499ccbcf82bbea181575c31d6/adapters/glm/mia-exl3-dflash2-dual). Its original patch-bundle metadata declares `MIT AND Apache-2.0` and attributes MiaAI-Lab, vLLM contributors, Turboderp and IncoAI. Existing comments and attribution remain intact. No model weights are included.

Local changes allow a bounded 4096–1000000-token context and 1–4 sequences, with initial defaults of 65536 tokens and two sequences. Utilization is reduced to 0.80 for initial qualification. Exact TP2/native-mp placement, Controller-injected fabric/rendezvous, pinned DFlash2 K7 specification, FP8 target KV and the K-pool tail slot-map fix remain enforced. The recipe corrects construction capabilities, runtime-interface labeling, exact artifact accounting and disk reservations. The source bundle and local patch entity are rehashed together.

The initial experimental resource contract reserves a 116 GB startup peak and 8 GB system reserve per Spark. At 0.80 utilization on the observed 130.66 GB host, vLLM's 104.53 GB target leaves approximately 11.47 GB inside that startup envelope for additional initialization overhead. This is an estimate to test, not measured memory qualification. The stock 0.87 profile requires its larger reservation and a separate recipe revision; it cannot be selected under this smaller envelope. Context limits above 65536 and sequence counts above two remain opt-in experiments, not established capabilities.

The image remains pinned to `ghcr.io/miaai-lab/glm-5.3-flash-2x-dgx-sparks@sha256:9bb1557a4234fce63d59599e44d10747eabd742beb337eebf9e7070be8a0fd58`, associated with MiaAI-Lab source `493cb88fc69f8ba73ac87404f429d763e2739d89`. The checked September 5 head `3021f24c88a0904c768c46ff22a508407e31360a` is 21 commits newer: it adds E2 fat-expert prefill, indexer workspace sizing, chat-template prefix-cache changes, spinwait controls and experimental TP4. This first Vonk integration experiment deliberately retains the compatible immutable catalog image; it cannot substantiate the newer E2 performance claims.

Upstream model terms are separate: Z.AI base GLM-5.3-Flash is MIT; the selected EXL3 checkpoint declares `LicenseRef-ShapleyMCG-1.0`, and the exact DFlash2 checkpoint declares `CC-BY-NC-ND-4.0`. No additional rights or model qualification are implied by this evaluation recipe.

The local runtime-distribution entity corrects the stock f1 interface assumptions to the actually observed primary `enp1s0f0np0` / `rocep1s0f0` RoCEv2 GID3 rail on both nodes. The f1 ports are down here. The secondary f0 rail is preserved but unused; host networking is not changed. These bindings are site-specific, and real two-rank NCCL/GPU execution remains unqualified.
