{
  "mcpServers": {
    "time": {
      "type": "stdio",
      "command": "uvx",
      "args": ["mcp-server-time"],
      "description": "Time/timezone utility. stdio runs inside the metamcp pod via the bundled uvx toolchain."
    },
    "digikey": {
      "type": "streamable_http",
      "url": "http://digikey-mcp.ai.svc.cluster.local:8000/mcp",
      "description": "DigiKey component search + MyLists (3-legged OAuth, rotating token cached on the backend PVC)."
    },
    "playwright-stealth": {
      "type": "streamable_http",
      "url": "http://playwright-stealth-mcp.ai.svc.cluster.local:8931/mcp",
      "description": "Stealth headless-browser automation. Highest prompt-injection surface; isolate in the 'browser' namespace."
    },
    "grafana": {
      "type": "streamable_http",
      "url": "http://grafana-mcp.ai.svc.cluster.local:8000/mcp",
      "description": "Read-only Grafana. Queries Prometheus/VictoriaMetrics/Loki/VictoriaLogs through Grafana datasources."
    },
    "flux-operator-mcp": {
      "type": "streamable_http",
      "url": "http://flux-operator-mcp.ai.svc.cluster.local:9090/mcp",
      "description": "Read-only Flux GitOps introspection. Traces ResourceSets/HelmReleases/Kustomizations/sources to workloads and pod logs; diffs Git vs cluster."
    },
    "kubernetes-mcp": {
      "type": "streamable_http",
      "url": "http://kubernetes-mcp.ai.svc.cluster.local:8080/mcp",
      "description": "Read-only Kubernetes introspection for the CRDs Flux doesn't model (Cilium, Longhorn, kaniop, Gateway API, Traefik, cert-manager, CNPG)."
    },
    "pcbparts-mcp": {
      "type": "streamable_http",
      "url": "http://pcbparts-mcp.ai.svc.cluster.local:8080/mcp",
      "description": "Read-only component search across JLCPCB in-stock DB + EasyEDA footprints + DigiKey + Mouser. JLCPCB/LCSC pricing and assembly stock."
    },
    "open-meteo-mcp": {
      "type": "streamable_http",
      "url": "http://open-meteo-mcp.ai.svc.cluster.local:3000/mcp",
      "description": "Read-only weather over the free Open-Meteo APIs (forecast/archive/air-quality/marine/geocoding). No API key."
    },
    "markitdown-mcp": {
      "type": "streamable_http",
      "url": "http://markitdown-mcp.ai.svc.cluster.local:3001/mcp",
      "description": "Read-only file/URL -> Markdown conversion (PDF/DOCX/XLSX/PPTX/images/etc.)."
    }
  }
}
